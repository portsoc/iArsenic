meta {
  name: login
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/v1/user/login
  body: json
  auth: none
}

body:json {
  {
    "email": "kane.swartz@port.ac.uk",
    "password": "{{test_password}}"
  }
}
