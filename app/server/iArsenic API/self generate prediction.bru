meta {
  name: self generate prediction
  type: http
  seq: 13
}

post {
  url: {{base_url}}/api/v1/self/well/{{well_id}}/predict
  body: none
  auth: none
}

headers {
  ~authorization: Bearer {{access_token}}
}
