{
  "name": "iarsenic-types",
  "version": "2.0.43",
  "description": "types used in iarsenic project",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "prepublishOnly": "npm run build"
  },
  "license": "ISC",
  "dependencies": {
    "module-alias": "^2.2.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
