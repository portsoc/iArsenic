{
  "name": "i-arsenic-server",
  "version": "0.0.1",
  "description": "cloud functions for backend processing in iArsenic",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gcloud functions deploy requests --runtime nodejs8 --trigger-http --entry-point requests"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/datastore": "^4.1.3"
  },
  "devDependencies": {
    "semistandard": "^13.0.1"
  }
}
