{
  "name": "i-arsenic-server",
  "version": "0.0.1",
  "description": "cloud functions for backend processing in iArsenic",
  "main": "index.js",
  "scripts": {
    "test": "npm run lint",
    "lint": "eslint *.js",
    "deploy": "gcloud functions deploy requests --runtime nodejs10 --trigger-http --entry-point requests"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/datastore": "^4.1.3",
    "simple-google-openid": "^0.3.0"
  },
  "devDependencies": {
    "semistandard": "^13.0.1"
  }
}
